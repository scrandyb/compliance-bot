"""Setup file for compliance bot repo. Use this to configure package

For local development, run `python setup.py develop`
"""

from setuptools import setup

setup(
    name = "compliance_bot",
    packages=['compliance_bot'],
)